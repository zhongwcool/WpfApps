<Window x:Class="App14.IASystem.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:App14.IASystem.ViewModels"
        mc:Ignorable="d"
        Title="IASystem" Height="800" Width="1200" Loaded="Window_Loaded">
    <Window.Resources>
        <Style TargetType="DataGrid">
            <Setter Property="AutoGenerateColumns" Value="False" />
            <Setter Property="CanUserAddRows" Value="False" />
            <Setter Property="CanUserDeleteRows" Value="False" />
        </Style>
    </Window.Resources>
    <Grid>
        <TabControl>
            <TabItem x:Name="PoolsTab" Header="鱼塘"
                     d:DataContext="{d:DesignInstance d:Type=vm:PoolsTabViewModel, IsDesignTimeCreatable=True}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <DataGrid Grid.Column="0"
                              ItemsSource="{Binding PoolsCollection}"
                              SelectedItem="{Binding SelectedPool, Mode=TwoWay}">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="鱼塘名" Binding="{Binding Name}" />
                            <DataGridTextColumn Header="基地" Binding="{Binding Farm.Name}" />
                            <DataGridTextColumn Header="创建日期"
                                                Binding="{Binding Path=AddDate, StringFormat={}{0:yyyy-MM-dd}}" />
                            <DataGridTextColumn Header="进度" Binding="{Binding Progress}" />
                            <DataGridTextColumn Header="设备数" Binding="{Binding Devices.Count}" />
                        </DataGrid.Columns>
                    </DataGrid>

                    <StackPanel Grid.Column="1">
                        <HeaderedContentControl Header="基本信息" Margin="0,5,0,0"
                                                Style="{StaticResource H3HeaderedContentControl}">
                            <Grid>
                                <Grid.Resources>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Margin" Value="5" />
                                    </Style>
                                </Grid.Resources>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="75" />
                                    <ColumnDefinition Width="75" />
                                    <ColumnDefinition Width="75" />
                                    <ColumnDefinition Width="75" />
                                    <ColumnDefinition Width="75" />
                                    <ColumnDefinition Width="75" />
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="30" />
                                    <RowDefinition Height="30" />
                                </Grid.RowDefinitions>
                                <TextBlock Grid.Row="0" Grid.Column="0" Text="鱼塘" />
                                <TextBlock Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" Text="{Binding SelectedPool.Name}" />
                                <TextBlock Grid.Row="0" Grid.Column="2" Text="进度" />
                                <TextBlock Grid.Row="1" Grid.Column="2" Grid.ColumnSpan="2" Text="{Binding SelectedPool.Progress}" />
                                <TextBlock Grid.Row="0" Grid.Column="4" Text="添加日期" />
                                <TextBlock Grid.Row="1" Grid.Column="4" Grid.ColumnSpan="2"
                                           Text="{Binding SelectedPool.AddDate, StringFormat={}{0:yyyy-MM-dd}}" />
                            </Grid>
                        </HeaderedContentControl>

                        <HeaderedContentControl Header="鱼塘说明" Margin="0,5,0,0"
                                                Style="{StaticResource H3HeaderedContentControl}">
                            <TextBlock Text="{Binding SelectedPool.Note}" Height="100" Margin="5" TextTrimming="CharacterEllipsis"
                                       TextWrapping="Wrap" />
                        </HeaderedContentControl>

                        <HeaderedContentControl Header="设备列表" Margin="0,5,0,0"
                                                Style="{StaticResource H3HeaderedContentControl}">
                            <DataGrid ItemsSource="{Binding SelectedPool.Devices}"
                                      SelectedItem="{Binding SelectedNode, Mode=TwoWay}">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="节点名" Binding="{Binding NodeName}" />
                                    <DataGridTextColumn Header="序列号" Binding="{Binding SerialNum}" />
                                    <DataGridTextColumn Header="创建日期"
                                                        Binding="{Binding Path=AddDate, StringFormat={}{0:yyyy-MM-dd}}" />
                                    <DataGridTextColumn Header="类型" Binding="{Binding ModelType}" />
                                    <DataGridTextColumn Header="协议版本" Binding="{Binding Protocol}" />
                                    <DataGridTextColumn Header="固件版本" Binding="{Binding Firmware}" />
                                </DataGrid.Columns>
                            </DataGrid>
                        </HeaderedContentControl>
                    </StackPanel>
                </Grid>
            </TabItem>

            <TabItem x:Name="DevicesTab" Header="设备"
                     d:DataContext="{d:DesignInstance d:Type=vm:DevicesTabViewModel, IsDesignTimeCreatable=True}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <DataGrid Grid.Column="0"
                              ItemsSource="{Binding NodesCollection}"
                              SelectedItem="{Binding SelectedDevice, Mode=TwoWay}">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="节点名" Binding="{Binding NodeName}" />
                            <DataGridTextColumn Header="序列号" Binding="{Binding SerialNum}" />
                            <DataGridTextColumn Header="创建日期"
                                                Binding="{Binding Path=AddDate, StringFormat={}{0:yyyy-MM-dd}}" />
                            <DataGridTextColumn Header="类型" Binding="{Binding ModelType}" />
                            <DataGridTextColumn Header="协议版本" Binding="{Binding Protocol}" />
                            <DataGridTextColumn Header="固件版本" Binding="{Binding Firmware}" />
                        </DataGrid.Columns>
                    </DataGrid>
                    
                    <StackPanel Grid.Column="1">
                        <HeaderedContentControl Header="基本信息" Margin="0,5,0,0"
                                                Style="{StaticResource H3HeaderedContentControl}">
                            <Grid>
                                <Grid.Resources>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Margin" Value="5" />
                                    </Style>
                                </Grid.Resources>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="75" />
                                    <ColumnDefinition Width="75" />
                                    <ColumnDefinition Width="75" />
                                    <ColumnDefinition Width="75" />
                                    <ColumnDefinition Width="75" />
                                    <ColumnDefinition Width="75" />
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="30" />
                                    <RowDefinition Height="30" />
                                </Grid.RowDefinitions>
                                <TextBlock Grid.Row="0" Grid.Column="0" Text="节点名" />
                                <TextBlock Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" Text="{Binding SelectedDevice.NodeName}" />
                                <TextBlock Grid.Row="0" Grid.Column="2" Text="设备类型" />
                                <TextBlock Grid.Row="1" Grid.Column="2" Grid.ColumnSpan="2" Text="{Binding SelectedDevice.ModelNum}" />
                                <TextBlock Grid.Row="0" Grid.Column="4" Text="添加日期" />
                                <TextBlock Grid.Row="1" Grid.Column="4" Grid.ColumnSpan="2"
                                           Text="{Binding SelectedDevice.AddDate, StringFormat={}{0:yyyy-MM-dd}}" />
                            </Grid>
                        </HeaderedContentControl>
                    </StackPanel>
                </Grid>
            </TabItem>
        </TabControl>
    </Grid>
</Window>