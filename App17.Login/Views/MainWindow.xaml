<Window x:Class="App17.Login.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:App17.Login.ViewModels"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance vm:MainWindowViewModel, IsDesignTimeCreatable=True}"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <WrapPanel Grid.Row="0">
            <Button Click="ButtonBase_OnClick" Margin="5">Login</Button>
            <Button Click="ButtonData_OnClick" Margin="5">数据</Button>
        </WrapPanel>
        <ScrollViewer Grid.Row="1">
            <TextBlock Name="TxtRepo" TextWrapping="Wrap" />
        </ScrollViewer>
    </Grid>
</Window>